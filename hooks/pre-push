#!/usr/bin/env bash
# Pre-push: run all gates and verify the ledger. Blocks a push on any FAIL/ERROR.
set -euo pipefail
export PYTHONPATH="src"
python -m selfproof.cli build
python -m selfproof.cli ledger verify
